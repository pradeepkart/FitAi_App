import{useEffect,useState}from'react';import api from'../services/api';import{useAuth}from'../context/AuthContext';import{Card,ProgressBar}from'../components/UI';export default function DashboardPage(){const{user}=useAuth(),[d,setD]=useState({workouts:[],water:[],calories:[],goals:[],bmi:{}});useEffect(()=>{Promise.all(['/workouts','/water','/calories','/goals','/fitness/bmi'].map(x=>api.get(x))).then(([w,wa,c,g,b])=>setD({workouts:w.data,water:wa.data,calories:c.data,goals:g.data,bmi:b.data})).catch(()=>{})},[]);const today=new Date().toISOString().slice(0,10),water=d.water.filter(x=>x.recordedDate===today).reduce((s,x)=>s+x.amountMl,0),cals=d.calories.filter(x=>x.recordedDate===today).reduce((s,x)=>s+x.calories,0);return <><div className="hero"><p>WELCOME BACK</p><h1>{user?.name} 👋</h1><span>Here’s how your fitness journey is moving today.</span></div><div className="grid four"><Card title="Current weight" value={`${user?.weight||'—'} kg`}/><Card title="BMI" value={d.bmi.bmi||'—'} sub={d.bmi.category}/><Card title="Today's calories" value={`${cals} kcal`}/><Card title="Total workouts" value={d.workouts.length}/></div><div className="grid two"><Card title="Hydration" value={`${water} / 3000 ml`}><ProgressBar value={water} max={3000}/></Card><Card title="Active goal" value={d.goals.find(x=>x.status==='ACTIVE')?.goalType?.replaceAll('_',' ')||'Set your first goal'}/></div><section className="card"><h2>Recent workouts</h2>{d.workouts.slice(0,4).map(x=><div className="list" key={x.id}><b>{x.workoutName}</b><span>{x.duration} min · {x.caloriesBurned} kcal</span></div>)}{!d.workouts.length&&<p className="muted">No workouts yet. Add one to begin your streak.</p>}</section></>}
