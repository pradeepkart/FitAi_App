package com.fitness.tracker.repository; import com.fitness.tracker.entity.Workout; import org.springframework.data.jpa.repository.JpaRepository; import java.util.*; public interface WorkoutRepository extends JpaRepository<Workout,Long>{List<Workout> findByUserIdOrderByWorkoutDateDesc(Long id); Optional<Workout> findByIdAndUserId(Long id,Long userId);}
